--- /Applications/Locomotive2/Bundles/standardRailsJan2007.locobundle/i386/lib/ruby/gems/1.8/gems/cached_model-1.3.1/lib/cached_model.rb	2007-03-24 14:13:39.000000000 +0300
+++ cached_model.rb	2007-03-25 15:59:40.000000000 +0400
@@ -146,7 +146,7 @@
   # Find by primary key from the cache.
 
   def self.find_by_sql(*args)
-    return super unless args.first =~ /^SELECT \* FROM #{table_name} WHERE \(#{table_name}\.#{primary_key} = '?(\d+)'?\) +LIMIT 1/
+    return super unless args.first =~ /^SELECT \* FROM #{table_name} WHERE \(#{table_name}\.#{primary_key} = '?(\d+)'?\)( +LIMIT 1)?/
 
     id = $1.to_i
