<% if current_user && user.id != current_user.id %>
	<%= link_to (current_user.subscribed_to?(user) ? '-' : '+'), global_url(:action => 'relationship', :user_id => user.id), :class => user.dom_id(:rel_toggle) %>
<% end %>
